# RSUE Backend API

Backend API для системы управления складами.

## 🚀 Быстрый старт с Docker
### Установка и запуск

1. **Клонируйте репозиторий** (если еще не клонирован):
```bash
git clone https://github.com/oriole-hub/rsue-backend
cd rsue_backend
```

2. **Создайте файл `.env`** :
```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

3. **Запустите приложение**:
```bash
docker-compose up -d
```

4. **Проверьте статус**:
```bash
docker-compose ps
```

5. **Откройте в браузере**:
- API документация: http://localhost:8000/papers
- Health check: http://localhost:8000/api/health

### Остановка

```bash
docker-compose down
```

### Остановка с удалением данных

```bash
docker-compose down -v
```

## 📋 Локальная разработка (без Docker)

### Требования

- Python 3.11+
- PostgreSQL 15+
- pip

### Установка

1. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. **Установите зависимости**:
```bash
pip install -r req.txt
```

3. **Настройте базу данных**:
```bash
# Создайте базу данных PostgreSQL
createdb rsue

# Или через psql:
psql -U postgres
CREATE DATABASE rsue;
CREATE USER tapok WITH PASSWORD 'your-password';
GRANT ALL PRIVILEGES ON DATABASE rsue TO tapok;
```

4. **Настройте переменные окружения**:
```bash
export DATABASE_URL="postgresql://tapok:chinazes778@localhost:5432/rsue"
```

5. **Запустите приложение**:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` или экспортируйте переменные:

- `DATABASE_URL` - URL подключения к PostgreSQL
- `DB_USER` - Пользователь БД (по умолчанию: `tapok`)
- `DB_PASSWORD` - Пароль БД (по умолчанию: `chinazes778`)
- `DB_NAME` - Имя БД (по умолчанию: `rsue`)
- `APP_PORT` - Порт приложения (по умолчанию: `8000`)

### Изменение настроек БД в коде

Если используете локальную разработку, отредактируйте `app/core/core.py`:

```python
SQLALCHEMY_DATABASE_URL = "postgresql://user:password@localhost/rsue"
```

## 📚 API Документация

После запуска приложения:

- **Swagger UI**: http://localhost:8000/papers

## 🗂️ Структура проекта

```
rsue_backend/
├── app/
│   ├── core/          # Основные настройки (БД, безопасность)
│   ├── models/        # SQLAlchemy модели
│   ├── routers/       # API роутеры
│   ├── services/      # Бизнес-логика
│   ├── utils/         # Утилиты
│   └── main.py        # Точка входа
├── static/            # Статические файлы (PDF, шрифты)
├── req.txt            # Python зависимости
├── Dockerfile         # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
└── README.md          # Этот файл
```

## 🔑 Основные endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Организации
- `POST /api/orga/create` - Создать организацию
- `GET /api/orga/me` - Моя организация

### Склады
- `GET /api/sklads` - Список складов
- `POST /api/sklads/create` - Создать склад

### Номенклатура
- `GET /api/nomen` - Список номенклатуры
- `POST /api/nomen/create` - Создать номенклатуру

### Документы склада
- `POST /api/docsklad/create` - Создать документ
- `GET /api/docsklad/list` - Список документов
- `PUT /api/docsklad/items/{item_id}` - Обновить элемент (чекер)

Подробнее: см. `DOCUMENT_LIFECYCLE.md`

## 🐳 Docker команды

### Сборка образа
```bash
docker-compose build
```

### Просмотр логов
```bash
docker-compose logs -f backend
docker-compose logs -f db
```

### Выполнение команд в контейнере
```bash
docker-compose exec backend bash
docker-compose exec db psql -U tapok -d rsue
```

### Пересборка после изменений
```bash
docker-compose up -d --build
```

## 🧪 Тестирование

```bash
# Запуск тестов (если есть)
pytest

# Или через Docker
docker-compose exec backend pytest
```

## 📝 Миграции базы данных

Таблицы создаются автоматически при первом запуске 

## 🐛 Troubleshooting

### Проблема: Не могу подключиться к БД

```bash
# Проверьте, что БД запущена
docker-compose ps

# Проверьте логи
docker-compose logs db

# Проверьте переменные окружения
docker-compose exec backend env | grep DB
```

### Проблема: Порт уже занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # Внешний порт:внутренний порт
```

### Проблема: Ошибки при установке зависимостей

```bash
# Пересоберите образ
docker-compose build --no-cache
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи: `docker-compose logs`
2. Статус контейнеров: `docker-compose ps`
3. Подключение к БД: `docker-compose exec db psql -U tapok -d rsue`


**Приятной разработки! 🦦**

